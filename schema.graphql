# The entities for CryptoKitties.
type KittyOwner @entity {
  id: ID! #entity id
  tokenId: BigInt! #token id
  owner: Bytes! #owner of the token
  contract: Bytes! #contract address
}

type KittyBalance @entity {
  id: ID!
  amount: BigInt!
}
